server:
  port: 8081

spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/cinema_db
    username: root
    password: root123
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
